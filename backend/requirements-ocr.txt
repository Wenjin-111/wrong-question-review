# OCR / PDF / 导出 相关重型依赖 — 阶段 4 之后需要时再安装
# 安装命令：uv pip install -r requirements-ocr.txt

pymupdf==1.25.*
fpdf2==2.8.*
pillow==11.1.*
numpy>=1.24,<3
# HunyuanOCR-1.5 本地推理（GPU，需先单独安装 CUDA 版 torch，见下方注释）
torch==2.11.0+cu128
torchvision==0.26.0+cu128
transformers==5.14.*
accelerate==1.14.*
