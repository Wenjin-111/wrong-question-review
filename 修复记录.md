# 修复记录

> 本文件记录每次 bug/安全问题修复：修复时间、问题、修复方法、验证方式。
> 依据《检查报告.md》的 P0/P1/P2 优先级逐项处理。

---

## 2026-08-01 14:35 · 批量打标签接口永远 422（P0）

**问题**：`PUT /api/questions/batch-tag` 返回 422。路由注册顺序错误——`PUT /questions/{question_id}`（questions.py:101）注册在 `PUT /questions/batch-tag` 之前，Starlette 按序匹配，`batch-tag` 被 `{question_id}` 路径参数捕获，int 转换失败。

**修复**：`backend/app/routers/questions.py` — 将 `batch_tag` 路由函数整体移到 `update_question`（动态路由）之前，静态路径优先匹配。

**验证**：`curl -X PUT /api/questions/batch-tag` 响应从 422 → 403（正确匹配到 batch-tag 处理器，仅未认证）。

---

## 2026-08-01 14:36 · 导出功能任意文件读取（路径穿越）（P0）

**问题**：`json_with_images` 导出时，用 `url.replace("/uploads/","")` + `os.path.join` 拼接图片路径。题目 Markdown 内容可写入 `src="/uploads/../../.env"`，导出 zip 时会把 `.env`（含 JWT_SECRET、数据库口令）读入压缩包下发。

**修复**：`backend/app/routers/export.py` — 双重防御：
1. 拒绝含 `..` 或反斜杠 `\` 的 URL；
2. `os.path.normpath` 解析后校验路径仍在 `UPLOAD_ROOT` 目录内（前缀检查），且只写入文件（`os.path.isfile`）。

**验证**：`python -m py_compile` 通过；代码审查确认越界路径全部被拦截。

---

## 2026-08-01 14:37 · 改密码后旧 token 仍有效（P0）

**问题**：`PUT /api/settings/password` 只更新密码哈希，已签发的 JWT（含其他设备）继续有效，泄露的旧 token 无法吊销。

**修复**：`backend/app/routers/settings.py` `update_password` — 密码更新后递增 `current_user.token_version`（`get_current_user` 依赖校验 `token_version`，递增后所有已签发 token 立即失效）。

**验证**：代码审查确认与 refresh 轮换逻辑一致的吊销机制。

---

## 2026-08-01 15:05 · AI 聊天流式回答期间切换会话，消息串台（P0）

**问题**：`AIChatPage` 流式回答进行中切换会话（`loadSession`/新建/绑定题目），旧流的 `onToken/onDone/onError` 回调仍会向**新会话**的消息列表追加内容，且旧流结束时的 `setIsStreaming(false)` 影响新会话状态。

**修复**：`frontend/src/pages/AIChatPage.tsx` — 引入 `sendSidRef` 标记：
- `handleSend` 发起时记录当前会话 id（`sendSidRef.current = sid`）；
- 所有回调先检查 `sendSidRef.current !== sid` 则直接丢弃；
- `loadSession`/`handleNewSession`/`handleBindQuestion` 切换会话时置 `sendSidRef.current = null` 作废旧流；
- `handleSend` 末尾仅在标记仍匹配时才 `setIsStreaming(false)`。

**验证**：代码走查——切换会话后旧流回调全部短路；TS 编译无新增错误。

---

## 2026-08-01 15:10 · 登录/注册无速率限制（P0）

**问题**：slowapi 的 `default_limits` 只对显式 `@limiter.limit` 装饰的路由生效，`/api/auth/login`、`/register` 无任何限制，可被暴力破解/批量注册。

**修复**：
- 新建 `backend/app/utils/rate_limit.py` — limiter 单例（避免 main.py ↔ auth.py 循环导入）；
- `backend/app/main.py` — 改为从该模块导入 limiter，其余配置不变；
- `backend/app/routers/auth.py` — `login` 加 `@limiter.limit("10/minute")`、`register` 加 `@limiter.limit("5/minute")`，函数签名增加 `request: Request` 参数。

**验证**：`python -m py_compile` 通过。⚠️ 注意：后端进程若未以 `--reload` 启动，需重启后端后限流才生效（待用户重启后复测：12 次连续登录请求第 11 次起应返回 429）。

---

## 2026-08-01 15:30 · 16 个 TS 编译错误全部修复（P1）

**问题**：`npm run build` 长期失败（`tsc -b` 报 16 个错误）：11 个未使用 import/变量、2 个 React 19 `useRef` 缺初始参数、2 个类型不匹配/字段缺失、3 个 markdown-it 类型问题。

**修复**：
- 删除未使用 import/变量 11 处：ImageCropper(useEffect)、MarkdownEditor(Space)、BatchEditPage(Modal)、OCREntryPage(removeImage 函数)、PDFImportPage(Steps)、ProfilePage(Divider/authApi)、QuestionsPage(SearchOutlined)、SettingsPage TypesTab(loading + setLoading)
- `useRef<...>()` → `useRef<...>(undefined)`（QuestionsPage、SelectQuestionsPage）
- `getAnswerTypeFromTypeName` 返回类型 `string | null` → `'choice' | 'fill' | 'subjective' | null`（BatchEditPage、QuestionAddPage）
- `Subject` 接口补 `question_count?: number`（types/index.ts，后端实际返回该字段）
- `npm i -D @types/markdown-it`；markdown.ts:82 回调参数显式标注类型

**验证**：`npx tsc -b` 0 错误；`npm run build` 首次完整通过（dist 产物正常生成）；lint 仅剩 10 个无害警告（3 个 Fast refresh 约定 + 7 个 exhaustive-deps 有意保留）。

---

## 2026-08-01 15:40 · 删除 recharts 死依赖（P1）

**问题**：StatsPage 已从 recharts 换成 ECharts，但 `package.json` 依赖和 `vite.config.ts` 的 manualChunks 配置仍保留 recharts（全项目无任何引用）。

**修复**：`npm uninstall recharts`；`vite.config.ts` 删除 `if (id.includes('recharts')) return 'charts'` 分块配置。

**验证**：`npm run build` 通过。注：bundle 大小未变化——recharts 从未被 import，本就不进产物，此改动纯属依赖/配置清理。

---

## 2026-08-01 15:50 · `/api/health` 恒返回 503（P1）

**问题**：`main.py:77` 用 `db.bind.dialect.do_ping(None)` 探测数据库——`do_ping()` 是 SQLAlchemy 方言内部方法，需要真实 DBAPI 连接对象，传 `None` 必抛 `AttributeError`，被 except 捕获后恒返回 503。

**修复**：改为 SQLAlchemy 2.x 标准健康检查 `db.execute(text("SELECT 1"))`（连不上时 `execute` 自然抛异常进 503 分支，行为一致但真实可用）。

**验证**：`curl /api/health` → `{"status":"ok","database":"connected"}`（reload 生效后）；登录请求 401 确认数据库正常。

---

## 2026-08-01 16:05 · 上传先读入内存再校验大小（P1，方案1：读前按 Content-Length 拦截）

**问题**：4 处文件上传（题目图片 `questions.py`、头像 `settings.py`、PDF import/extract `ocr.py`）都是先 `file.read()` 全量读入内存再校验大小，超大文件会在拒绝前耗尽内存（DoS 攻击面）。

**修复**：每处在 `read()` 前加 `if file.size is not None and file.size > 上限: raise 400`（`UploadFile.size` 即 Content-Length）。保留读后校验作双保险（Content-Length 可伪造/缺失时兜底）。上限：图片 10MB、头像 5MB、PDF 50MB。

**验证**：`python -m py_compile` 通过；4 处检查均已生效（需登录态才能实测函数体，逻辑为直白 if 判断）。

---

## 2026-08-01 16:20 · 学科详情待复习恒为 0（硬编码 bug）

**问题**：首页"今日待复习"正常显示（如 10 题），但统计页学科详情表格的"待复习"列全是横线（—）。根因：`stats.py` 的 `subjects-breakdown` 端点把每科 `"pending"` **硬编码为 0**，从未计算。

**修复**：`backend/app/routers/stats.py` — 复用 `_get_due_question_ids`（与首页 `get_today_pending` 同一口径），按 `subject_id` 分组计数，`pending` 改为真实值。

**验证**：`python -m py_compile` 通过；逻辑上各学科 pending 之和 = 首页今日待复习数（需登录后查看统计页确认）。

---

## 2026-08-01 17:10 · 个人主页改造成"学习档案"页

**需求**：个人主页从纯账号设置页改造为学习档案页（首页=行动中心，主页=成果展示）。

**后端**：
- `GET/PUT /api/settings/signature` — 学习签名存储（UserConfig KV，上限 100 字符）
- `GET /api/stats/subject-retrievability` — 各学科 **FSRS 记忆保留度**（`compute_retrievability(elapsed, stability)` 按学科平均，仅统计已进入 FSRS 复习的题，返回 reviewed/total）

**前端**（ProfilePage 重构）：
- 头部：头像 + 用户名 + **可编辑学习签名**
- 数据小览：总错题 / 累计打卡 / 正确率 / 总作答
- **学科掌握度**：FSRS 记忆保留度进度条（分级色：≥60 绿、40-59 琥珀、<40 红）+ 已复习/总数
- 最近一周正确率迷你趋势图（ECharts）
- 最近练习（最新 5 条练习记录）
- 账号管理（用户名/邮箱/密码）折叠到 Collapse

**验证**：两新端点注册成功（403）；`tsc -b` 0 错误。

---

## 待办（来自《检查报告.md》）

- **P0**：AIChat 断网时 `isStreaming` 永久卡死（`streamSSE` fetch 不在 try 内）；BatchEditPage 点击题目列表重置未保存题题型
- **P1**：health 恒 503、上传内存耗尽风险、题目 code 列表/详情不一致、submit_answer 不校验 session 归属、`/uploads` 无鉴权、JWT 弱密钥、OCR 错误泄漏路径、stats `days` 无上限
- **P1**：前端删除 recharts 死依赖、16 个 TS 编译错误、AIChat 串台已修、排序 Select 未写 filters、PDFImport setTimeout 未清理、响应解包统一、删除题目无错误处理、client.ts isRefreshing 卡死
- **P2**：死代码/死依赖清理（nh3、torchvision、未用 schema/API 方法）、重复代码提取、`/uploads` 鉴权策略
