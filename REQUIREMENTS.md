# 错题收集与重做系统 - 需求文档

## 1. 项目概述

一个支持多用户的在线错题管理系统。核心流程：**收集错题 → 按遗忘曲线重做 → 追踪统计 → 逐步掌握**。辅以 AI 答疑和导出功能。

---

## 2. 用户系统

### 2.1 注册
- 字段：用户名、邮箱、密码、确认密码
- 校验：用户名 2-20 字符且唯一，邮箱格式校验且唯一，密码最低 6 字符
- 注册成功后自动登录并跳转到首页

### 2.2 登录
- 支持用户名或邮箱 + 密码登录
- 登录成功后返回 JWT Token（access token + refresh token）
- access token 过期时间 30 分钟，refresh token 过期时间 7 天
- 前端自动用 refresh token 续期 access token

### 2.3 登出
- 清除本地 Token，跳转到登录页

### 2.4 用户设置
- 修改密码（需输入旧密码）
- 修改用户名、邮箱
- 配置 AI API Key（可选，不配置则无法使用 AI 答疑功能）
- 配置 AI 模型偏好（API 地址、模型名称）

---

## 3. 页面结构 & 导航

### 3.1 整体布局
```
┌──────────────────────────────────────┐
│  Logo    导航菜单    [用户头像下拉]    │  ← 顶部导航栏
├──────────┬───────────────────────────┤
│ 侧边栏   │                           │
│ ·首页   │      内容区域              │
│ ·错题库 │                           │
│ ·重做   │                           │
│ ·统计   │                           │
│ ·设置   │                           │
├──────────┴───────────────────────────┤
│  不展示 Footer，页面内容区域滚动      │
└──────────────────────────────────────┘
```

### 3.2 页面路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `/login` | 登录页 | 未登录时所有路由重定向至此 |
| `/register` | 注册页 | |
| `/` | 首页/仪表盘 | 今日概览、待复习提醒 |
| `/questions` | 错题库 | 错题列表、搜索、筛选、CRUD |
| `/questions/add` | 添加错题 | 手动录入（含 OCR / PDF 入口） |
| `/questions/:id` | 错题详情 | 查看单题详情、AI 答疑 |
| `/review` | 重做中心 | 选择模式与筛选条件 |
| `/review/session` | 重做进行中 | 逐题作答 |
| `/review/result` | 重做结果 | 本次重做总结 |
| `/stats` | 统计页 | 全局 + 各学科统计图表 |
| `/settings` | 设置页 | 用户信息、学科/题型、遗忘曲线、AI 配置 |

### 3.3 权限控制
- 未登录：只能访问 `/login` 和 `/register`
- 已登录：所有页面可访问
- 用户数据完全隔离，接口只返回当前用户的数据

---

## 4. 学科与题型管理

### 4.1 学科 CRUD
- 位于设置页 → "学科管理"标签页
- 新建学科：输入学科名称（如"高等数学"），选择图标或颜色标记
- 编辑/删除学科：删除学科时，若该学科下仍有错题则禁止删除，提示用户先将所有错题删除（错题列表页提供"全选删除"按钮）。若学科下无错题则可直接删除，关联题型一并级联删除
- 学科列表展示：名称、错题数、题型数

### 4.2 题型 CRUD
- 位于设置页 → "题型管理"标签页
- 每个题型属于一个学科
- 新建题型：输入题型名称（如"多选题"、"判断题"），选择所属学科
- 系统在用户首次注册时自动创建 4 个默认学科（数学、英语、物理、化学），每个学科带 4 个默认题型（选择题、填空题、主观题、客观题），用户可自由修改删除

### 4.3 标签管理
- 位于设置页 → "标签管理"标签页
- 用户自定义标签（如"高考真题"、"易错"、"公式类"）
- 标签颜色标记
- 标签 CRUD，删除标签仅解除关联不影响题目

---

## 5. 错题收集

### 5.1 手动输入
**页面布局**：左侧表单 + 右侧实时预览

**表单字段**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 所属学科 | 下拉选择 | 是 | 从用户已创建的学科中选择 |
| 题型 | 级联选择 | 是 | 根据所选学科动态加载题型列表 |
| 题目内容 | 富文本编辑器 | 是 | 支持文字、图片插入、公式（LaTeX） |
| 正确答案 | 取决于题型 | 是 | 见下方说明 |
| 解析 | 富文本编辑器 | 否 | 解题思路、知识点讲解 |
| 来源 | 文本输入 | 否 | 如"2024高考数学卷·第5题" |
| 标签 | 多选标签 | 否 | 从已有标签选择或即时新建 |

**正确答案字段按题型区分**：
- 选择题：输入选项列表（A/B/C/D...可增删），勾选正确选项
- 填空题：文本输入框，多个空用分隔符或用 `[空1] 答案1 [空2] 答案2` 格式
- 主观题/客观题：文本输入框（富文本），用于自评时参考

**交互细节**：
- 添加错题成功后弹窗提示，提供两个按钮："继续添加"和"查看题目"
- 富文本中的图片上传为即时上传（选择图片后立刻上传到服务器，返回 URL 插入编辑器）
- 支持键盘快捷键：Ctrl+Enter 提交保存

### 5.2 拍照 OCR + AI 智能解析

**整体流程**：图片框选 → OCR 文字识别 → AI 结构化解析 → 用户校对确认

**草稿暂存**：在 OCR → AI 解析 → 校对确认的任意步骤，用户可随时点击"保存草稿"按钮。草稿保存当前已填入表单的所有数据（学科、题型、OCR文本、AI解析结果、标签、来源等），即使中途断网或关闭浏览器，下次进入添加页面时可选择"继续编辑草稿"或"放弃草稿开始新的录入"。每个用户最多保留 5 份草稿。

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 上传图片  │ → │ 框选区域  │ → │ OCR 识别 │ → │ AI 解析  │ → 用户确认
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

#### 5.2.1 上传与框选

1. **上传图片**：用户选择或拖拽图片到上传区（支持 jpg/png/bmp/webp，单张最大 10MB）
2. **图片预览 & 框选**：
   - 上传后进入"框选模式"页面，大图居中展示
   - 用户拖拽鼠标在图片上画出一个矩形选区，框选出包含题目的有效区域
   - 选区可自由调整：拖拽四角缩放、拖拽内部移动位置
   - 提供旋转按钮（顺时针/逆时针 90°），用于修正横竖方向
   - 图片过大时默认缩放适配屏幕，支持滚轮缩放
   - 底部提供"使用整张图片"快捷按钮（跳过框选）
3. **确认框选**：点击"开始识别"，前端将选区坐标 + 旋转角度发送到后端（后端按坐标裁剪后再做 OCR）。若未框选则整张图片送入 OCR

#### 5.2.2 OCR 文字识别

4. **后端 OCR 处理**：
   - 收到图片后（若带选区坐标，先用 Pillow 裁剪），调用 PaddleOCR 进行文字识别
   - 返回结果：`[{text, confidence, bbox}, ...]` 按阅读顺序排列
   - 对低置信度（<0.7）的文字片段做标记，前端展示时高亮提示用户注意校对
   - 图片中嵌入的公式/图形区域无法识别，前端在对应位置显示"[图片]占位符"

5. **前端展示 OCR 原始结果**：
   - 以文本框展示 OCR 识别到的纯文本内容
   - 低置信度文字用黄色背景高亮标注
   - 用户可以在此步骤直接编辑修正 OCR 结果，再交给 AI 解析

#### 5.2.3 AI 结构化解析

6. **发送给 AI 解析**：
   - 用户点击"AI 智能解析"按钮
   - 将 OCR 原始文本（经用户初步修正后的版本）发送给 AI，附带如下提示词：

   ```
   你是一个错题解析助手。请从以下OCR识别的文本中，提取出三个部分：
   1. 题目内容（题干，包含选项等）
   2. 正确答案
   3. 答案解析（解题思路或知识点说明）

   如果某一部分在原文本中没有找到，请留空。

   请严格按以下JSON格式返回，不要添加任何其他内容：
   {
     "question": "提取的题目内容",
     "answer": "提取的正确答案",
     "explanation": "提取的答案解析"
   }

   以下是OCR识别文本：
   ---
   {OCR原始文本}
   ---
   ```

   - 使用用户在设置中配置的 AI API 进行解析
   - 未配置 AI API Key 时，跳过此步骤，OCR 结果直接填入"题目内容"字段，用户手动填写其余部分

7. **AI 解析结果回填**：
   - 解析成功 → 三个部分分别回填到录入表单的对应字段：
     - `question` → 题目内容（富文本）
     - `answer` → 正确答案
     - `explanation` → 解析（富文本）
   - 某字段为空 → 对应输入框保持空白，placeholder 显示"未识别到，请手动填写"
   - 选择题型检测：AI 返回的题目中如包含 A/B/C/D 选项模式，自动切换题型为选择题并解析出选项列表

#### 5.2.4 用户核对确认

8. **校对确认页**：
   - 左侧：原始图片（保留框选区域供对照）
   - 右侧：录入表单，各字段已由 AI 解析结果预填充
   - 用户逐字段校对修改：
     - 题目内容是否完整、是否有 OCR 错误
     - 正确答案是否正确（关键核对项，高亮提示）
     - 解析是否合理
   - 补充 AI 未识别到的信息：选择所属学科、题型、添加标签、填写来源

9. **提交保存**：
   - 点击"确认保存"完成错题录入
   - 若有必填字段为空，表单校验拦截，提示用户补充
   - 保存成功后弹窗："继续录入" / "查看题目"

#### 5.2.5 异常处理

| 场景 | 处理方式 |
|------|----------|
| 图片过大 | 前端压缩至 2000px 宽以内再上传 |
| OCR 识别为空 | 提示"未识别到文字，请确认框选区域是否包含文字，或尝试手动输入" |
| OCR 识别置信度整体偏低 | 顶部提示"识别效果可能不佳，建议仔细校对" |
| AI 未配置 | 跳过 AI 解析步骤，OCR 结果直接填入题目内容，其余字段手动填写 |
| AI 解析超时（30s） | 提示"AI 解析超时，可重试或跳过，手动填写" |
| AI 返回格式异常 | 提示"AI 返回格式异常"，将 OCR 原文填入题目内容，可手动调整后重新解析 |
| 整体失败 | 不阻塞流程，切换为纯手动输入模式，图片作为题目配图保留 |

### 5.3 PDF 导入
**流程**：
1. 用户上传 PDF 文件（最大 50MB）
2. 后端用 PyMuPDF 提取文本和图片，返回每页的内容（文本 + 图片列表）
3. 前端展示分页预览，用户逐页查看
4. 用户选中某一页或某几页，点击"导入为题目"
5. 选中页面的文本内容自动进入录入流程

**AI 智能解析**：
- PDF 提取后的文本同样支持 AI 智能解析（复用 5.2.3 的解析流程）
- 用户可点击"AI 智能解析"将选中的 PDF 文本结构化提取为题目、答案、解析三部分
- 流程：PDF 提取文本 → 用户校对修正 → AI 解析 → 回填表单 → 用户确认保存

**批量模式**：
- 用户可勾选多页，一次性批量创建多道题目
- 批量创建时，每页作为一道独立题目，共用相同的学科和题型设置
- 支持"批量 AI 解析"：依次对每页内容调用 AI 解析（需 AI 已配置），解析进度逐条展示
- 批量模式下每道题默认使用相同的学科和题型，可在确认页逐题微调

---

## 6. 错题库（题目管理）

### 6.1 列表页
- 表格展示，列：题目摘要、学科、题型、标签、正确率、遗忘曲线阶段、最近作答时间、录入时间
- 筛选项：学科、题型、标签、正确率区间、录入时间范围
- 关键词搜索：搜索题目内容（MySQL LIKE / 全文索引）
- 排序：按录入时间、正确率、最近作答时间排序
- 分页：每页 20 条
- 单行操作：查看详情、编辑、删除（软删除）
- 批量操作：批量删除、批量修改标签、批量导出

### 6.2 详情页
- 展示完整题目信息（题目、答案、解析、来源、标签、录入时间）
- 展示该题统计：总次数、正确次数、正确率、遗忘曲线阶段、历史作答记录时间线
- AI 答疑入口（展开/收起对话面板）

### 6.3 编辑
- 与录入表单结构一致，可修改所有字段

---

## 7. 错题重做

### 7.1 重做中心（准备页）
用户选择以下条件发起一轮重做练习：

| 筛选条件 | 说明 |
|----------|------|
| 学科 | 必选，可多选 |
| 题型 | 可选，不选=全部题型 |
| 标签 | 可选，不选=全部标签 |
| 数量限制 | 可选，最多 N 题，不填=全部符合条件的题 |
| 最低正确率 | 可选，只做正确率低于 X% 的题 |
| 重做模式 | 自由模式 / 遗忘曲线模式 |
| 题目顺序 | 随机打乱 / 按录入顺序 / 按遗忘紧急度排序 |

**遗忘曲线模式额外规则**：
- 只筛选"当前到达复习时间点"的题目
- 例如：某题上次答对后进入第 3 阶段（1 天后），当前时间已超过上次作答 + 1 天，则该题进入待复习队列
- 答对：推进到下一阶段，该题按新阶段的时间间隔等待下次复习
- 答错：回退 2 个阶段（至少回退到第 1 阶段），例如原在第 5 阶段答错→回退到第 3 阶段，原在第 2 阶段答错→回退到第 1 阶段。同时错误次数 +1。避免一次失误就完全重置，减少挫败感

### 7.2 重做作答页
**逐题展示模式**（一次只显示一道题）：
- 顶部：进度条（当前题号 / 总题数）
- 中间：题目内容（富文本渲染 + 图片查看）
- 下方：作答区（按题型不同）：
  - 选择题：渲染选项按钮，点击选择，支持修改
  - 填空题：输入框，多个空位对应多个输入框
  - 主观题/客观题：大文本输入框
- 操作按钮："提交答案" → 显示结果（正确/错误/待自评）

**提交后**：
- 正确：绿色提示，显示解析，按钮显示"下一题"
- 错误：红色提示，高亮正确答案，显示解析，按钮显示"下一题"
- 主观题/客观题：显示参考答案，用户自行判断对错（"我答对了"/"我答错了"两个按钮），然后"下一题"

**完成全部题目后**：跳转到结果页

### 7.3 重做结果页
- 本次练习总结：总题数、正确数、错误数、正确率
- 按学科的统计（如果选择多个学科）
- 题目列表（每题显示作答结果、正确答案、解析）
- "再来一轮"按钮（复用相同筛选条件）
- "返回重做中心"按钮

---

## 8. AI 答疑

### 8.1 入口
- 错题详情页：题目下方固定"AI 答疑"折叠面板
- 重做作答页：每道题提交后在解析区域旁有"问 AI"按钮

### 8.2 对话面板
- 侧边弹出或展开面板，显示多轮对话
- 首次打开时，自动发送一段系统提示给 AI，包含题目上下文：
  ```
  用户正在复习一道{学科}的{题型}题目。
  题目内容：{题目内容}
  正确答案：{正确答案}
  解析：{解析}
  请针对这道题涉及的知识点，以答疑老师的身份帮助用户理解。
  ```
- 系统提示在每次对话会话中**仅发送一次**（首次打开面板或清空对话后重新开始时），后续追问仅发送用户消息和 AI 回复历史，不再重复发送题目上下文
- 用户输入问题（如"这个公式是怎么推导的？"）
- AI 回复，支持流式输出（逐字显示）
- 用户可继续追问

### 8.3 配置要求
- 使用 OpenAI 兼容 API 格式（/v1/chat/completions）
- 用户在设置页填写：API 地址、API Key、模型名称
- 没有配置 API Key 时，对话面板显示"请先在设置中配置 AI API Key"
- 每条对话消息存储到数据库，下次打开该题可以看到历史对话

### 8.4 异常处理
- 网络超时（30s）：提示用户稍后重试
- API 返回错误：显示具体错误信息
- API Key 无效：提示检查配置

---

## 9. 数据统计

### 9.1 首页仪表盘
轻量概览，进入即见：

- **今日待复习**：大数字卡片，今日有多少道题需要复习（基于遗忘曲线到期）
- **本周正确率**：折线图，本周每天的正确率变化
- **学科分布**：饼图，各学科错题数量占比
- **最近添加**：最近 5 道新增错题的列表（点击跳转详情）
- **薄弱学科提示**：正确率最低的 3 个学科高亮

### 9.2 统计页
更详细的数据分析：

**概览卡片行**：
| 卡片 | 内容 |
|------|------|
| 总错题数 | 数字 + 较上周变化量 |
| 总作答次数 | 历史累计 |
| 总体正确率 | 百分比 |
| 连续打卡天数 | 每天至少做一道题算打卡，从今天往前推算连续打卡天数（基于 review_record.created_at 按日期去重后计算连续天数） |

**图表区域**：
- 正确率趋势图（折线图）：可按"近 7 天 / 30 天 / 全部"切换
- 学科错题分布（饼图）
- 各学科正确率对比（柱状图）
- 每日做题量（柱状图）
- 各题型正确率对比（雷达图）

**列表区域**：
- 各学科详细统计表格：学科名、错题数、正确率、待复习数、今日需复习数

### 9.3 数据刷新
- 统计数据在服务端做短时间缓存（默认 60 秒 TTL），避免高并发时重复执行相同的聚合查询
- 涉及较大时间范围时后端做数据库层聚合查询
- 缓存对用户透明，用户在统计页刷新时最多看到 60 秒前的数据快照

---

## 10. 错题导出

### 10.1 导出入口
- 错题库列表页 → "批量导出"按钮
- 选择导出范围：全部 / 按筛选条件 / 手动勾选

### 10.2 导出内容
- 每道题包含：题目内容、答案、解析、学科、题型、标签、来源、正确率、录入时间
- 题目中的图片嵌入 PDF

### 10.3 导出格式
- PDF 文件（A4 纸张），排版整洁，适合打印
- 支持生成"试卷模式"（只显示题目 + 作答区，答案和解析独立一页）和"解析模式"（题目 + 答案 + 解析完整展示）

### 10.4 数据可移植性导出
- 设置页 → "数据管理"标签页 → "导出全部数据"
- 将用户所有数据导出为一个 JSON 文件，包含：用户基本信息、所有错题（题目内容、答案、解析、来源、标签）、所有作答记录、学科/题型配置
- 导出格式为结构化 JSON，可被本系统重新导入（导入功能作为后续版本规划）
- 导出文件中的图片以 URL 引用，不嵌入 base64（防止文件过大）
- 同时支持"导出并包含图片"（打包为 ZIP，JSON + 图片文件夹）
- 目的：满足数据可移植性需求，用户可备份或迁移到其他平台

---

## 11. 设置页

### 11.1 个人设置
- 修改用户名、邮箱
- 修改密码（旧密码 + 新密码 + 确认新密码）

### 11.2 学科管理（见第 4 节）

### 11.3 题型管理（见第 4 节）

### 11.4 遗忘曲线设置
- 展示当前阶段列表，每个阶段：阶段序号、阶段名称、时间间隔（分钟/小时/天可切换单位）
- 默认 6 个阶段：20分钟 → 1小时 → 1天 → 2天 → 6天 → 31天
- 用户可增删阶段、修改间隔时长
- **修改间隔的影响范围**：修改后的间隔仅对后续完成的题目生效，已排期的 `sr_next_review` 保持不变。如需全部重新计算，可使用"重置并重新排期"按钮（需二次确认）
- 重置为默认值按钮

### 11.5 AI 配置

同一套 AI 配置同时用于两个功能：**AI 答疑**（第 8 节）和 **OCR/PDF 智能解析**（第 5.2、5.3 节）。未配置时两个功能均不可用。

- API 地址输入框（默认 `https://api.openai.com/v1`，兼容所有 OpenAI 格式的 API）
- API Key 输入框（密码隐藏显示，支持 OpenAI / Claude / 本地大模型等）
- 模型名称输入框（默认 `gpt-4o`）
- "测试连接"按钮：发送一条测试消息，验证配置是否正确可用
- 提示文字：说明此密钥仅存储在本地服务器，仅用于错题解析和答疑

---

## 12. 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端框架 | React 18+ | Vite 构建 |
| UI 组件库 | Ant Design 5.x | 表格、表单、布局等开箱即用 |
| 路由 | React Router v6 | |
| 状态管理 | React Context + useReducer | 轻量，无需引入 Redux |
| 富文本编辑 | TipTap | 支持图片、LaTeX 公式 |
| 富文本渲染 | TipTap (readonly) | 统一渲染引擎 |
| 图表 | Recharts | |
| HTTP 客户端 | Axios | 拦截器处理 JWT 续期 |
| 后端框架 | FastAPI | Python 3.10+ |
| ORM | SQLAlchemy 2.0 | |
| 数据库迁移 | Alembic | |
| 数据库 | MySQL 8.0 | |
| 认证 | python-jose (JWT) | access + refresh token |
| OCR | PaddleOCR | 离线免费，中文识别效果好 |
| PDF 解析 | PyMuPDF | 提取文本和图片 |
| 图片存储 | 本地文件系统 | 配置存储根路径 |
| AI 接口 | httpx 调用 OpenAI 兼容 API | 流式 SSE 透传 |
| 密码加密 | passlib + bcrypt | |
| HTML 清洗 | nh3 | bleach 已停止维护，nh3 更快且活跃维护 |
| 日志 | loguru | 结构化日志，支持按级别输出 |

---

## 13. 非功能需求

### 13.1 性能
- 页面首屏加载 < 3 秒
- 接口响应 < 500ms（OCR 除外，可放宽至 10s）
- 列表分页，单次返回 ≤ 20 条

### 13.2 安全
- 密码 bcrypt 加盐哈希
- JWT 过期自动续期
- 用户数据隔离（API 查询强制带 user_id）
- 图片上传限制类型和大小（jpg/png/bmp，≤10MB）
- PDF 上传限制大小（≤50MB）
- SQL 注入防护（ORM 参数化查询）
- XSS 防护（富文本内容清洗前后端双重处理）
- AI API Key 使用 Fernet 对称加密存储于数据库，运行时由服务端密钥解密
- API 频率限制：登录接口 5次/分钟/IP，AI 相关接口 20次/分钟/用户，通用接口 120次/分钟/用户

### 13.3 日志与监控
- 后端使用 loguru 结构化日志，按级别输出到控制台和文件（按天轮转，保留 30 天）
- 关键节点记录日志：用户登录/登出、错题 CRUD、OCR 识别耗时与置信度、AI API 调用耗时与状态码、异常堆栈
- OCR/AI 流水线每个步骤记录耗时，便于定位性能瓶颈
- 日志级别：开发环境 DEBUG，生产环境 INFO
- 前端错误通过 `window.onerror` 捕获并上报到后端 `/api/log/frontend`（静默发送，不阻塞用户操作）

### 13.4 可用性
- 所有删除操作需二次确认
- 表单必填项标注红色星号
- 操作成功/失败有 Toast 提示
- 加载状态显示 Loading / Skeleton 占位
- 空状态显示引导提示（如"还没有错题，点击添加"）

### 13.5 兼容性
- 支持 Chrome、Edge 最新两个大版本
- 屏幕分辨率 ≥ 1280×720（电脑端优先）

### 13.6 部署与运行环境
- 提供 Docker Compose 一键部署方案（MySQL + FastAPI + React）
- PaddleOCR 模型在 Docker 镜像构建时预下载，避免首次使用时的长时间等待
- 图片存储目录通过环境变量配置，支持挂载宿主机目录做持久化
- 建议定期备份 `uploads/` 目录和 MySQL 数据库（提供备份脚本）
