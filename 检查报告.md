# 错题收集系统 · 代码检查报告

> 检查时间：2026-08-01
> 范围：frontend/src（React 19 + AntD v6 + Vite）、backend/app（FastAPI + SQLAlchemy + MySQL）
> 标注：[已验证] 表示已通过代码复现/实际请求确认；其余为静态审查发现

---

## 一、高严重度（建议尽快修复）

### 后端

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 1 | **导出功能任意文件读取（路径穿越）** [已验证] | `backend/app/routers/export.py:89-93` | `json_with_images` 导出用 `url.replace("/uploads/","")` + `os.path.join` 拼接图片路径。题目 Markdown 内容可写入 `src="/uploads/../../.env"`，导出 zip 时会把 `.env`（含 JWT_SECRET/数据库口令）读入压缩包。 |
| 2 | **批量打标签接口永远 422（路由顺序错误）** [已验证] | `backend/app/routers/questions.py:101,124` | `PUT /questions/{question_id}` 注册在 `PUT /questions/batch-tag` 之前，Starlette 按序匹配，"batch-tag" 无法转 int → 批量打标签功能不可用。 |
| 3 | **登录/注册无速率限制** | `backend/app/main.py` + `routers/auth.py` | slowapi 只作用于显式装饰的路由，`/api/auth/login`、`/register` 可被暴力破解/批量注册。 |
| 4 | **改密码后旧 token 仍有效** | `backend/app/routers/settings.py:230-236` | 修改密码不递增 `token_version`/`token_family`，泄露的旧 token 无法吊销。 |

### 前端

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 5 | **断网/异常时聊天输入区永久禁用** | `frontend/src/pages/AIChatPage.tsx:105` | `streamSSE` 的 fetch 不在 try 内，请求异常时 `isStreaming` 永远为 true，输入框和发送按钮永久禁用。 |
| 6 | **流式回答期间切换会话，消息串台** | `frontend/src/pages/AIChatPage.tsx:54-63` | 切换会话后旧流的 `onDone` 仍会把回复追加进新会话的消息列表。 |
| 7 | **点击题目列表会重置所有未保存题目的题型** | `frontend/src/pages/BatchEditPage.tsx:367` | 点击左侧题目即触发 `handleSubjectChange`，把全局设置（含题型）应用到所有未保存题目，`globalSubject` 为空时传 0 清空题型列表。 |

---

## 二、中严重度（建议修复）

### 后端

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 8 | `/api/health` 恒返回 503 | `backend/app/main.py:77` | `db.bind.dialect.do_ping(None)` 必抛异常，健康检查不可用。 |
| 9 | 上传先整体读入内存再校验大小 | `questions.py:135-137`、`settings.py:247-249`、`ocr.py:100-102` | 超大文件可致内存耗尽；且只校验扩展名/Content-Type，不校验真实文件内容。 |
| 10 | 题目 code 在列表页与详情页不一致 | `backend/app/services/question_service.py:223-239` | `row_number()` 只在当前 IN 集合内编号，列表（20 条）与详情（1 条）算出不同的 code。 |
| 11 | 提交答案不校验题目归属 | `backend/app/services/review_service.py:150-259` | 任意题目可提交进任意 session 并累加计数；同题可重复提交，统计失真。 |
| 12 | `/uploads` 静态目录无鉴权 | `backend/app/main.py:60-64` | 知道 URL 即可访问他人上传的错题图片（隐私泄漏）。 |
| 13 | JWT_SECRET 弱密钥且校验失效 | `backend/app/config.py` + `.env` | `dev-jwt-secret-change-in-production` 是弱密钥，但启动校验只拦截 `change-me-in-production`，形同虚设。 |
| 14 | OCR 错误信息泄漏内部路径 | `backend/app/routers/ocr.py:50,68,87,167` | `str(e)` 拼接进响应，泄漏服务端路径/堆栈。 |
| 15 | `days` 参数无上限 | `backend/app/routers/stats.py:98` | 可传超大值造成循环 DoS。 |

### 前端

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 16 | 选择题提交调用了两次 submitAnswer | `ReviewSessionPage.tsx:277-306` | 第一次无判分作答先入库，第二次（带判分）失败时页面报"提交失败"但第一次已计入。 |
| 17 | 排序 Select 未写入 filters | `QuestionsPage.tsx:230` | `defaultValue` 只改界面，请求仍用后端默认排序，显示与请求不一致。 |
| 18 | `setTimeout` 导航无清理 | `PDFImportPage.tsx:60` | 期间返回/换文件会被旧定时器强制跳转。 |
| 19 | API 响应解包模式混乱 | `src/api/` 各文件 | subjects/tags 直接 `setSubjects(data)`，review/stats/draft/chat 用 `data.data \|\| data`，AppearanceTab 又是第三种写法，易错。 |
| 20 | 删除题目无错误处理 | `QuestionsPage.tsx:94,100` | 失败静默，用户以为删成功。 |
| 21 | refresh_token 缺失时 `isRefreshing` 未重置 | `src/api/client.ts:51-59` | 标志永久卡死（有整页跳转兜底，影响有限）。 |
| 22 | 游戏24开关可能恒为关 | `Game24Provider.tsx:21` | `data.enabled` 取值与后端裸响应一致（已验证无问题，但需保持两端同步，勿改动响应包装）。 |

---

## 三、低严重度 / 多余代码（建议清理）

### 前端

| # | 问题 | 位置 |
|---|---|---|
| 23 | **recharts 死依赖** | `package.json:32` + `vite.config.ts:26`（StatsPage 已换 echarts，全项目无 recharts 引用） |
| 24 | 16 个 TS 编译错误（unused import 为主） | 见下清单 |
| 25 | API 未使用方法：`auth.refresh`、`review.todayPending`、`draft.convert`、`ocr.extractPdf`、`chat.sendMessage`、`subjects.updateType` | `src/api/*.ts` |
| 26 | 重复代码：`getAnswerTypeFromTypeName`（QuestionAddPage:70 = BatchEditPage:280）、"按题型名合并"（QuestionsPage:79 = SelectQuestionsPage:51）、答案 JSON 渲染（QuestionDetailPage:105 = ReviewSessionPage:116）、导出 blob 下载（SettingsPage:607 = QuestionsPage:110） | 各文件 |
| 27 | `DashboardPage:146-149` 热力图 O(n²) 且每格同色 | 首页 |
| 28 | `App.tsx:43/52/53` 路由缩进错乱；`AppLayout:45-48` logout 未 await | 布局 |
| 29 | `.section-title` CSS 类无任何页面引用 | `styles/global.css` |

**TS 编译错误明细（16 个）**：ImageCropper:1（useEffect）、MarkdownEditor:2（Space）、BatchEditPage:5（Modal）及 :461（类型）、OCREntryPage:74（removeImage）、PDFImportPage:3（Steps）、ProfilePage:2/6（Divider/authApi）、QuestionsPage:4（SearchOutlined）及 :46（useRef 缺参）、SelectQuestionsPage:43（同）、SettingsPage:274（question_count 字段不存在）及 :303（loading）、markdown.ts:1（缺 @types/markdown-it）及 :82（隐式 any）。

### 后端

| # | 问题 | 位置 |
|---|---|---|
| 30 | 未使用 import | `draft.py:11,13`、`question_service.py:4`、`review_service.py:1,4`、`ai_service.py:6`、`security.py:4`、`settings.py:5` |
| 31 | 死代码/未用 schema：`SessionOut`/`SubmitResult`/`SessionSummary`、`CreateSessionRequest.min_accuracy`、`stats.py:182 pending=0` | `schemas/review.py`、`routers/stats.py` |
| 32 | 死依赖：`nh3`（全项目无引用）、`torchvision`（未 import）、`accelerate`（仅间接） | `requirements*.txt` |
| 33 | `QuestionImage.question_id` 从未赋值，图片永不关联题目且无清理 | `models/` + 上传逻辑 |
| 34 | 改头像不删旧文件（磁盘泄漏）；`os.makedirs("logs")` 相对路径依赖 CWD | `settings.py`、`main.py:18` |
| 35 | `@app.on_event` 已弃用 | `main.py:67` |
| 36 | `ocr_service.py:24-25` crop 参数无校验，负值/浮点直接 500 | OCR 服务 |
| 37 | `auth.py:54-59` refresh 返回结构与 `TokenResponse` 不一致（缺 user 字段） | 鉴权 |

---

## 四、修复优先级建议

**P0（安全/功能不可用，先修）**
1. export.py 路径穿越 → 用 `os.path.basename` + 白名单校验 `/uploads/` 前缀
2. batch-tag 路由顺序 → 静态路由注册在动态路由之前
3. AIChatPage 断网卡死 + 串台 → fetch 包 try/finally + 会话切换时中止旧流
4. BatchEditPage 点击重置题型 → 点击题目不再触发全局设置变更

**P1（健壮性/隐私）**
5. 登录限流、health 修复、改密码吊销 token
6. 上传改为流式/先读头部再限大小
7. submit_answer 校验 session 归属、去重
8. 删除 recharts 依赖、清理 16 个 TS 错误（可让 lint/build 变绿）

**P2（清理/一致性）**
9. 响应解包统一、删除死代码/死依赖、重复逻辑提取
10. /uploads 鉴权（需设计公开分享策略后再定）
